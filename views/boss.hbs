<script src="../js/boss.js"></script>
<div class="container">
  <div class="characteristics">
    <h2>Игроки команды {{data.name}}</h2>
    <ul class="changeChar">
      {{#each data.players}}
      <li>{{login}}
        <form id="{{_id}}" method="POST">
          {{!-- <input type="text" placeholder="{{value}}"> --}}
          <button type="submit">+</button>
        </form>
      </li>
      {{/each}}
      <div class="logout">

        <button onclick="addCh('{{data.name}}')" type="button">addCh</button>
{{!-- <input value="Нажми меня" onclick="addCh('{{data.name}}')" type="button"> --}}
      </div>
    </ul>
  </div>
  {{!-- <div class="items">
    <h2>Инвентарь {{player.login}}</h2>
    <ul>
      {{#each player.inventory}}
      <li id="{{_id}}"> {{name}}, weight - {{weight}}</li>
      {{/each}}

  </div> --}}
  <div class="teamItems">
    <h2>Инвентарь команды {{data.name}}</h2>
    <ul>
      {{#each data.storage}}
      <li id="{{_id}}"> {{name}}, weight - {{weight}}
        <form id="{{_id}}" method="POST">
          {{!-- <input type="text" placeholder="{{value}}"> --}}
          <button type="submit">Ok</button>
        </form>
      </li>
      {{/each}}
    </ul>
  </div>

  <div class="logout">
    <form method="GET" action="/logout">
      <button type="submit">LogOut</button>
    </form>
  </div>
  <div class="logout">
    <form method="GET" action="/logout">
      <button type="submit">LogOut</button>
    </form>
  </div>
  <link rel="stylesheet" href="/stylesheet/account.css">
